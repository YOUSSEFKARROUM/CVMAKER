# ===========================================
# Template des variables d'environnement
# Copier vers .env et remplir les valeurs
# NE JAMAIS committer .env (déjà dans .gitignore)
# ===========================================

# Firestore (local storage)
VITE_DISABLE_FIRESTORE=true

# ========== Keycloak ==========
# IMPORTANT: VITE_KEYCLOAK_URL = URL du SERVEUR Keycloak (auth)
# Ex: https://auth.tondomaine.com ou https://keycloak.example.com/auth
# PAS l'URL de l'app Vercel/Railway !
VITE_KEYCLOAK_URL=https://votre-serveur-keycloak.com/auth
VITE_KEYCLOAK_REALM=cv-maker
VITE_KEYCLOAK_CLIENT_ID=cv-maker-client

# Client Secret (Keycloak Admin → Clients → cv-maker-client → Credentials)
VITE_KEYCLOAK_CLIENT_SECRET=your-client-secret

# Admin Client (pour inscription directe)
VITE_KEYCLOAK_ADMIN_CLIENT_ID=cv-maker-admin
VITE_KEYCLOAK_ADMIN_CLIENT_SECRET=your-admin-secret

# Backend (proxy) pour les appels admin Keycloak depuis le front
# Exemple: backend Node/Express déployé sur Railway
VITE_BACKEND_URL=https://votre-backend-cvmaker.com

# Mode local (sans Keycloak)
VITE_LOCAL_MODE=false

# ========== Backend (app/backend) ==========
# Ces variables sont lues par server.mjs (pas de préfixe VITE_)
# À configurer sur Railway (ou autre) pour le service backend.

# URL du serveur Keycloak (même valeur que VITE_KEYCLOAK_URL, sans /auth si votre URL l'inclut déjà)
KEYCLOAK_URL=https://votre-serveur-keycloak.com/auth
KEYCLOAK_REALM=cv-maker
KEYCLOAK_ADMIN_CLIENT_ID=cv-maker-admin
KEYCLOAK_ADMIN_CLIENT_SECRET=your-admin-secret

# Origines CORS autorisées (séparées par des virgules). En production, définir les URLs du front (ex: https://ton-app.vercel.app)
# Si vide, toutes les origines sont acceptées (à éviter en production).
ALLOWED_ORIGINS=
